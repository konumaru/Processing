/*
add だと、足し算になる (add(int, int), sub(), mult(), div() )
increment() で１追加、大きさが１でもいいならリストの追加もできる
min() , max() で最大最小がわかる
sort(), 小さい順に並び替え ( sortReverse() で大きい順 ) 
reverse(), shule() でリストをいじれる
append() で新しく追加、clear() で全消去、size(), 
hasValue() でその数がListに入っているか確認
set( int(何番目に入れるか), int(入れたい数)
*/
